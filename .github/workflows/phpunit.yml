name: Laravel CI (PHPUnit / Pest)

on:
  push:
    branches:
      - 'main'
      - 'develop'
    paths-ignore:
      - 'docs/**'
      - '**/*.md'
  pull_request:
    paths-ignore:
      - 'docs/**'
      - '**/*.md'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  DB_USERNAME: sail
  DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
  DB_HOST: 127.0.0.1
  DB_PORT: 3306
  DB_DATABASE: ledgerleap_test
  MAIL_FROM_ADDRESS: info@example.com

# ───────────────────────────────────────────────
# 共通セットアップは .github/actions/laravel-test-setup/action.yml に集約
# 各ジョブは uses: ./.github/actions/laravel-test-setup で呼び出す（Sprint 3）
# ───────────────────────────────────────────────
jobs:

  # ── ① Unit テスト（外部サービス不要・高速） ──────────────────
  unit:
    name: Unit Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30
    services:
      mysql:
        image: groonga/mroonga:mysql-8.0-latest
        ports:
          - 3306:3306
        env:
          MYSQL_DATABASE: ledgerleap_test
          MYSQL_USER: sail
          MYSQL_PASSWORD: ${{ secrets.DB_PASSWORD }}
          MYSQL_ROOT_PASSWORD: ${{ secrets.DB_PASSWORD }}
          MYSQL_ALLOW_EMPTY_PASSWORD: 1
        options: >-
          --health-cmd "mysqladmin ping"
          --health-interval 15s
          --health-timeout 5s
          --health-retries 10

    steps:
      - uses: actions/checkout@v4

      - name: Laravel Test Setup
        uses: ./.github/actions/laravel-test-setup
        with:
          db_password: ${{ secrets.DB_PASSWORD }}

      - name: Run Unit Tests
        # external グループ（外部コンテナ依存）は除外する
        # database-migrations グループ（DatabaseMigrations使用・他テストのDB状態を破壊）は除外する
        # --display-errors でスタックトレースを出力（デバッグ用）
        run: ./vendor/bin/pest --testsuite=Unit --exclude-group=external --exclude-group=database-migrations --display-errors

  # ── ② Feature テスト（DB必須・時間がかかる） ────────────────
  feature:
    name: Feature Tests
    runs-on: ubuntu-latest
    timeout-minutes: 40  # 実績 ~24分のため余裕を持たせて40分に設定
    services:
      mysql:
        image: groonga/mroonga:mysql-8.0-latest
        ports:
          - 3306:3306
        env:
          MYSQL_DATABASE: ledgerleap_test
          MYSQL_USER: sail
          MYSQL_PASSWORD: ${{ secrets.DB_PASSWORD }}
          MYSQL_ROOT_PASSWORD: ${{ secrets.DB_PASSWORD }}
          MYSQL_ALLOW_EMPTY_PASSWORD: 1
        options: >-
          --health-cmd "mysqladmin ping"
          --health-interval 15s
          --health-timeout 5s
          --health-retries 10

    steps:
      - uses: actions/checkout@v4

      - name: Laravel Test Setup
        uses: ./.github/actions/laravel-test-setup
        with:
          db_password: ${{ secrets.DB_PASSWORD }}

      - name: Run Feature Tests
        # external グループ（VLM/LDAP/ocrmypdf等の外部コンテナ依存）は除外する
        # database-migrations グループ（DatabaseMigrations使用・他テストのDB状態を破壊）は除外し、専用ジョブで実行する
        # --display-errors でスタックトレースを出力（デバッグ用）
        run: ./vendor/bin/pest --testsuite=Feature --exclude-group=external --exclude-group=database-migrations --display-errors

  # ── ③ DatabaseMigrations テスト（migrate:fresh が必要なテスト群） ──────
  #
  # 実行対象（`#[Group('database-migrations')]` を付与したテストのみ）:
  #   - FolderTest                    (マルチテナント境界の検証)
  #   - SearchApiTest                 (Mroonga 検索API統合)
  #   - LedgerFullTextSearchTest      (Mroonga 全文検索 - トランザクションロールバック不可)
  #   - SearchControllerAdditionalTest (全文検索コントローラ)
  #
  # なぜ分離するか:
  #   DatabaseMigrations は tearDown() で migrate:rollback を実行し全テーブルをdropする。
  #   これが同一プロセス内の RefreshDatabaseWithTenant 使用テストの DB 状態を破壊するため、
  #   独立したジョブ（独立したプロセス・コンテナ）で実行する必要がある。
  #
  db-migrations:
    name: DB Migrations Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30
    services:
      mysql:
        image: groonga/mroonga:mysql-8.0-latest
        ports:
          - 3306:3306
        env:
          MYSQL_DATABASE: ledgerleap_test
          MYSQL_USER: sail
          MYSQL_PASSWORD: ${{ secrets.DB_PASSWORD }}
          MYSQL_ROOT_PASSWORD: ${{ secrets.DB_PASSWORD }}
          MYSQL_ALLOW_EMPTY_PASSWORD: 1
        options: >-
          --health-cmd "mysqladmin ping"
          --health-interval 15s
          --health-timeout 5s
          --health-retries 10

    steps:
      - uses: actions/checkout@v4

      - name: Laravel Test Setup
        uses: ./.github/actions/laravel-test-setup
        with:
          db_password: ${{ secrets.DB_PASSWORD }}

      - name: Run DB Migrations Tests
        # DatabaseMigrations を使うテストのみ実行する
        # このジョブは独立したコンテナで実行されるため migrate:rollback による他テストへの影響なし
        # --display-errors でスタックトレースを出力（デバッグ用）
        run: ./vendor/bin/pest --group=database-migrations --display-errors
