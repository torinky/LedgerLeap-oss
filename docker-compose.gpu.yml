services:
  vlm:
    build:
      args:
        PADDLE_VERSION: gpu
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
