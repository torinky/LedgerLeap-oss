services:
  openldap:
    image: rroemhild/test-openldap
    ports:
      - '${FORWARD_LDAP_PORT:-389}:10389' # rroemhild/test-openldapのデフォルトポートは10389
      # LDAPSは通常636だが、rroemhild/test-openldapはLDAP_PORT=10389で動作しているため、ここでは明示しない
    networks:
      - sail
