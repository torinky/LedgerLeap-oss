{
  "$schema": "https://raw.githubusercontent.com/google-gemini/gemini-cli/main/schemas/settings.schema.json",
  // Copy this file to `settings.json` in the copied harness workspace.
  // This template assumes remote MCP over HTTP.
  // Use a tenant-resolving host in httpUrl (for example tenant-name.localhost),
  // not a bare localhost that bypasses domain-based tenancy resolution.
  // LedgerLeap web app must already be reachable at that endpoint.
  // Replace every __PLACEHOLDER__ before starting Gemini CLI.
  "privacy": {
    "usageStatisticsEnabled": false
  },
  "ui": {
    "hideContextSummary": false,
    "showHomeDirectoryWarning": true
  },
  "context": {
    "fileName": [
      "GEMINI.md"
    ],
    "includeDirectoryTree": true,
    "discoveryMaxDirs": 50,
    "includeDirectories": [],
    "loadMemoryFromIncludeDirectories": false,
    "fileFiltering": {
      "respectGitIgnore": true,
      "respectGeminiIgnore": true
    }
  },
  "skills": {
    "enabled": true,
    "disabled": []
  },
  "mcpServers": {
    "ledgerleap-api": {
      "httpUrl": "__LEDGERLEAP_MCP_HTTP_URL__",
      "headers": {
        "Authorization": "Bearer __LEDGERLEAP_MCP_BEARER_TOKEN__"
      },
      "timeout": 30000,
      "trust": false,
      "description": "LedgerLeap remote MCP evaluation server"
    }
  }
}

