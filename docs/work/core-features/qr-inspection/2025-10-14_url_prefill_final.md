# URLクエリパラメータによる台帳初期値設定機能 - 最終実装計画

**作成日:** 2025年10月14日  
**機能名:** URLクエリパラメータによる台帳カラム初期値設定  
**ステータス:** 実装計画フェーズ  
**対象ブランチ:** feature/qr-code-inspection

---

## 1. 概要

### 1.1 コンセプト

台帳登録画面で入力内容から事前入力リンクを生成し、クリップボードにコピーすることで効率的な繰り返し登録を実現します。

**特徴:**
- URLをクリップボードにコピー（QRコード化や共有方法はユーザー判断）
- 自動生成値（入力者名、日時、自動採番）は初期値と異なる場合のみURLに含める
- 台帳登録画面だけで完結

### 1.2 基本フロー

```
【URL作成】
台帳登録画面 → 想定値を入力 → 「事前入力リンクを取得」ボタン
→ URLをクリップボードにコピー → 必要に応じてQRコード化やメール送信

【URL使用】
リンクをクリック（またはQRコード読み取り） → ブラウザで台帳作成画面が開く（初期値設定済み）
→ 内容確認・修正 → 登録
```

---

## 2. 機能要件

### 2.1 URLパラメータ形式

**基本形式:**
```
https://{domain}/{tenant}/ledger/create/{ledgerDefineId}?prefill[{カラムID}]={値}
```

**例:**
```
/ledger/create/5?prefill[1]=EQ-001&prefill[2]=3F空調機A&prefill[5]=正常
```

### 2.2 対応カラムタイプ

| カラムタイプ | 対応 | URL生成時の処理 |
|------------|------|----------------|
| text | ✅ | 入力値をそのまま出力 |
| number | ✅ | 入力値をそのまま出力 |
| YMD / YMDHM | ✅ | **初期値と異なる場合のみ出力** |
| select / chk | ✅ | 選択値をそのまま出力 |
| textarea | ✅ | 入力値をそのまま出力 |
| user_name | ✅ | **初期値と異なる場合のみ出力** |
| auto_number | ❌ | 常に自動生成（URL出力不可） |
| files | ❌ | セキュリティ上非対応 |

### 2.3 自動生成値の扱い

**対象カラム:**
- `user_name`: ログインユーザーから自動生成
- `YMD/YMDHM`: 現在日時を自動設定するカラム
- `auto_number`: 自動採番

**ルール:**
- URL生成時: 初期値と同じ値の場合はURLに含めない
- URL解釈時: パラメータがない場合は自動生成ロジックを実行

**理由:**
- 点検者は毎回変わるため、QRコードに特定の人名を埋め込むと不適切
- 日時も同様に、記録日は実際の作業日であるべき

**例:**
```javascript
// URL生成時の判定
if (column.type === 'user_name' || column.type === 'YMD' || column.type === 'YMDHM') {
    const initialValue = getInitialValue(column);
    if (content[column.id] === initialValue) {
        // 初期値と同じ → URLに含めない
        continue;
    }
}
```

---

### 2.4 新規カラムタイプ: user_name（入力者名）

**オプション設定:**

```json
{
  "type": "user_name",
  "options": {
    "name_format": "full_name",        // or "family_name_only"
    "org_prefix": "bottom_only",       // or "none", "bottom_3_levels"
    "edit_mode": "overwrite"           // or "append"
  }
}
```

**動作:**
- 新規作成時: ログインユーザー情報から自動生成
- 編集時:
  - `overwrite`: 現在のユーザーで上書き
  - `append`: カンマ区切りで追加（重複チェックあり）

**表示例:**
- フルネーム + 最下層組織: `営業一課 山田太郎`
- 苗字のみ + 組織なし: `山田`
- 複数人（appendモード）: `山田, 佐藤, 鈴木`

**実装:**
- `app/Models/ColumnTypes/UserNameType.php` を新規作成
- 組織階層の取得ロジック
- 編集時の追加/上書きロジック

---

## 3. 実装詳細

### 3.1 CreateController の拡張

```php
public function create(CreateRequest $request)
{
    $ledgerDefine = LedgerDefine::findOrFail($request->ledgerDefineId);
    
    // 権限チェック
    // ...
    
    // prefillパラメータを検証
    $prefillParams = $this->validatePrefillParams(
        $request->query('prefill', []), 
        $ledgerDefine
    );
    
    return view('ledger.create', [
        'ledgerDefineRecord' => $ledgerDefine,
        'prefillParams' => $prefillParams,
    ]);
}

private function validatePrefillParams(array $params, LedgerDefine $ledgerDefine): array
{
    // カラムIDとタイプに応じたバリデーション
    // XSS対策、文字列長制限、選択肢チェック等
    // ...
}
```

### 3.2 CreateColumn の拡張

```php
public array $prefillParams = [];

public function mount(int $ledgerDefineId, array $prefillParams = []): void
{
    $this->prefillParams = $prefillParams;
    // ...
    $this->initColumns();
    $this->applyPrefillParams(); // prefillを適用
    // ...
}

protected function applyPrefillParams(): void
{
    foreach ($this->prefillParams as $columnId => $value) {
        if (array_key_exists($columnId, $this->content)) {
            $this->content[$columnId] = $value;
        }
    }
}

// 事前入力リンク生成メソッド
public function generatePrefillLink(): void
{
    $params = [];
    
    foreach ($this->content as $columnId => $value) {
        if ($this->isEmptyValue($value)) continue;
        
        $column = $this->getColumnById($columnId);
        
        // 自動生成カラムは初期値と異なる場合のみ含める
        if ($this->isAutoGeneratedColumn($column)) {
            $initialValue = $this->getInitialValue($column);
            if ($value === $initialValue) {
                continue; // 初期値と同じなので含めない
            }
        }
        
        // パラメータに追加
        if (is_array($value)) {
            foreach ($value as $item) {
                $params["prefill[{$columnId}][]"] = $item;
            }
        } else {
            $params["prefill[{$columnId}]"] = $value;
        }
    }
    
    // URL生成
    $this->generatedURL = route('ledger.create', [...]) . '?' . http_build_query($params);
    $this->showURLModal = true;
}

protected function isAutoGeneratedColumn($column): bool
{
    return in_array($column->type, ['user_name', 'YMD', 'YMDHM', 'auto_number']);
}

protected function getInitialValue($column)
{
    // カラムタイプに応じた初期値を取得
    // user_name: 現在のログインユーザーから生成
    // YMD: now()->format('Y-m-d')
    // YMDHM: now()->format('Y-m-d H:i')
    // ...
}
```

### 3.3 UserNameType の実装

```php
namespace App\Models\ColumnTypes;

class UserNameType implements InputType
{
    public array $options;
    
    public function __construct(array $options = [])
    {
        $this->options = array_merge([
            'name_format' => 'full_name',
            'org_prefix' => 'none',
            'edit_mode' => 'overwrite',
        ], $options);
    }
    
    public function generateValue(\App\Models\User $user): string
    {
        $name = $this->formatUserName($user);
        $orgPrefix = $this->getOrganizationPrefix($user);
        return $orgPrefix ? "{$orgPrefix} {$name}" : $name;
    }
    
    public function generateEditValue(\App\Models\User $user, ?string $currentValue): string
    {
        $newValue = $this->generateValue($user);
        
        if ($this->options['edit_mode'] === 'append' && !empty($currentValue)) {
            $existingNames = array_map('trim', explode(',', $currentValue));
            if (!in_array($newValue, $existingNames, true)) {
                $existingNames[] = $newValue;
                return implode(', ', $existingNames);
            }
            return $currentValue;
        }
        
        return $newValue;
    }
    
    // formatUserName(), getOrganizationPrefix() など
    // ...
}
```

### 3.4 UI実装

**ボタン追加（台帳登録画面）:**
```blade
<button wire:click="generatePrefillLink" class="btn btn-outline btn-info">
    <i class="fas fa-link"></i>
    事前入力リンクを取得
</button>
```

**モーダル:**
```blade
<x-modal wire:model="showURLModal" title="事前入力リンク">
    <div class="form-control">
        <label class="label">
            <span class="label-text">このリンクで台帳作成画面を開くと、現在の入力内容が自動入力されます</span>
        </label>
        <input type="text" value="{{ $generatedURL }}" readonly class="input input-bordered" />
    </div>
    
    <div class="modal-action">
        <button wire:click="copyToClipboard" class="btn btn-primary">
            <i class="fas fa-copy"></i>
            クリップボードにコピー
        </button>
        <button wire:click="$set('showURLModal', false)" class="btn">閉じる</button>
    </div>
    
    <div class="alert alert-info mt-4">
        <i class="fas fa-info-circle"></i>
        <span>コピーしたリンクはQRコード化したり、メールで送信したりできます</span>
    </div>
</x-modal>
```

**クリップボードコピー処理（Alpine.js）:**
```javascript
public function copyToClipboard(): void
{
    $this->dispatch('copy-to-clipboard', url: $this->generatedURL);
}
```

```blade
@script
<script>
    $wire.on('copy-to-clipboard', (event) => {
        navigator.clipboard.writeText(event.url).then(() => {
            $wire.dispatch('mary-toast', {
                type: 'success',
                title: 'コピーしました',
                description: 'クリップボードにリンクをコピーしました'
            });
        }).catch(() => {
            $wire.dispatch('mary-toast', {
                type: 'error',
                title: 'コピー失敗',
                description: 'クリップボードへのコピーに失敗しました'
            });
        });
    });
</script>
@endscript
```

---

## 4. 実装スケジュール

### Day 1（8時間）
- UserNameType 実装（2.5時間）
- CreateController の validatePrefillParams 実装（2時間）
- CreateColumn の applyPrefillParams 実装（1.5時間）
- 動作確認（2時間）

### Day 2（8時間）
- generatePrefillLink 実装（自動生成値の判定含む）（2.5時間）
- UI実装（ボタン、モーダル、クリップボードコピー）（2.5時間）
- テスト実装（3時間）

### Day 3（4時間）
- 統合テスト（2時間）
- ドキュメント整備（1時間）
- 最終確認（1時間）

**合計: 20時間（2.5日間）**

---

## 5. テスト計画

### 5.1 機能テスト

```php
// prefillパラメータが正しく適用されるか
public function test_prefill_params_are_validated_and_applied()

// 不正なカラムIDは無視されるか
public function test_invalid_column_id_is_ignored()

// XSS攻撃が防がれるか
public function test_xss_attack_is_prevented()
```

### 5.2 UserNameType テスト

```php
// フルネーム生成
public function test_generate_value_with_full_name()

// 苗字のみ生成
public function test_generate_value_with_family_name_only()

// 組織名プレフィックス（最下層のみ）
public function test_generate_value_with_org_prefix_bottom_only()

// 組織名プレフィックス（3階層）
public function test_generate_value_with_org_prefix_bottom_3_levels()

// 編集時の上書きモード
public function test_generate_edit_value_with_overwrite_mode()

// 編集時の追加モード
public function test_generate_edit_value_with_append_mode()

// 重複防止
public function test_generate_edit_value_with_append_mode_prevents_duplicate()
```

### 5.3 URL生成テスト

```php
// 通常の値はURLに含まれる
public function test_generate_url_includes_normal_values()

// 自動生成値が初期値と同じ場合はURLに含まれない
public function test_generate_url_excludes_auto_generated_default_values()

// 自動生成値が初期値と異なる場合はURLに含まれる
public function test_generate_url_includes_auto_generated_modified_values()

// 空の値はURLに含まれない
public function test_generate_url_excludes_empty_values()
```

---

## 6. ユースケースカバー範囲

| ユースケース | カバー度 | 備考 |
|------------|---------|------|
| UC1: 通常点検 | ✅ 100% | 完全対応 |
| UC2: 異常発見 | ✅ 95% | 通知以外は対応 |
| UC3: オフライン | ❌ 0% | Phase 2以降 |
| UC4: QRコード破損 | ✅ 100% | 手動入力で対応 |
| UC5: 一括点検 | 🔶 50% | 基本フローのみ |
| UC6: 初心者利用 | ✅ 90% | ほぼ対応 |
| UC7: 部分更新 | ✅ 100% | 既存機能で対応 |
| UC8: 点検スキップ | ✅ 100% | 完全対応 |

**総合カバー率: 79% / コアユースケース: 99%**

---

## 7. リスクと対策

### リスク1: 長いURLでQRコードが複雑化
- **対策:** 必要最小限のカラムのみ使用を推奨、Phase 2でURL短縮検討

### リスク2: 選択肢変更で既存QRコードが無効化
- **対策:** バリデーションで不正値を検出、ログに記録

### リスク3: 自動生成値の判定ミス
- **対策:** 厳密な初期値比較ロジック、十分なテストケース

---

## 8. まとめ

### 実装内容
1. URLパラメータによる台帳カラム初期値設定
2. 台帳登録画面で「事前入力リンクを取得」ボタン
3. クリップボードコピー機能（QRコード化や共有はユーザー判断）
4. user_name カラムタイプ（入力者名自動設定）
5. **自動生成値は初期値と異なる場合のみURLに含める**

### 実装期間
**2.5日間（20時間）**

### 次のステップ
1. 実装開始
2. 実証実験（設備点検での検証）
3. フィードバック収集
4. Phase 2検討（URL短縮、通知など）

---

**文書履歴:**
- 2025-10-14: 最終実装計画作成
  - 自動生成値は初期値と異なる場合のみURL出力
  - user_name カラムタイプ追加
  - ドキュメント簡潔化
