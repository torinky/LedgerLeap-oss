# 2025年 フェーズ3：Ruri v3活用によるエンハンスドベクトルインデックス戦略

**作成日:** 2025年11月24日
**更新対象:** [2025-11-15_vlm-ocr-and-indexing-strategy-review.md](./2025-11-15_vlm-ocr-and-indexing-strategy-review.md) の改訂版
**ステータス:** 計画策定完了・実装待ち

> **📖 関連ドキュメント:**
> - [VLM-OCR技術とベクトルインデックス高度化戦略](./2025-11-15_vlm-ocr-and-indexing-strategy-review.md)
> - [RAG機能導入に関する技術検討](../rag-implementation/2025-10-16-rag-implementation-study.md)

---

## 1. 戦略改訂の背景

当初のフェーズ3計画では、複数のベクトルを併用するハイブリッド検索と、UIの統合を構想していた。
しかし、以下の理由から方針を調整する。

1.  **意味検索の認知向上を優先:** ユーザーに「なぜこのレコードがヒットしたのか」という新しい検索体験を学習させるため、あえてUIは統合せず、意味検索の精度向上にリソースを集中する。
2.  **Ruri v3の性能活用:** 最新の埋め込みモデル **Ruri v3 (cl-nagoya/ruri-v3-310m)** の「指示追従能力」と「長大なコンテキスト長」を活かし、単一ベクトル内での表現力を最大化する「エンハンスドベクトルインデックス戦略」へ移行する。

---

## 2. 技術基盤の検証：Ruri v3の実力

### 2.1. モデル仕様と優位性
現在システムで稼働中のモデル `cl-nagoya/ruri-v3-310m` は、以下の仕様を持つ。

*   **最大シーケンス長:** 8192トークン (旧来の512トークンから大幅拡張)
*   **次元数:** 768次元
*   **インストラクション追従:** クエリとパッセージに特定のプレフィックス（`検索クエリ:`, `検索文書:`）を付与することで、検索意図を汲んだベクトル生成が可能。

### 2.2. 懸念事項の解消 (Token Limit & Metadata)
*   **懸念:** チャンク化する際、本文に加えて抽出キーワードなどのメタデータを埋め込むと、コンテキスト長（以前は512トークン）を超過し、情報が切り捨てられるリスクがあった。
*   **検証結果:** v3モデルの **8192トークン** 対応により、このリスクは完全に解消された。現在のチャンクサイズ設定（2000文字）に、数百文字のメタデータを付与しても十分な余裕がある。

---

## 3. エンハンスドベクトルインデックス・アーキテクチャ

### 3.1. 戦略の核心：メタデータ注入型チャンキング

専用のベクトルカラムを追加する代わりに、**チャンクテキスト自体に構造化メタデータを注入**してからベクトル化を行う。これは添付ファイルだけでなく、**台帳レコード本体**に対しても適用する。

#### チャンク構成イメージ (台帳本体・添付ファイル共通)
Ruriモデルに対して、以下のような構造化テキストを入力とする。

```text
検索文書: 【メタデータ】
キーワード: 請求書, 株式会社A商事, 2025年11月, サーバー保守費用
ステータス: 承認済み
フォルダ: 経理部/2025年度/請求書
作成者: 山田太郎
文書タイプ: 台帳レコード (または 添付ファイル: invoice.pdf)

【本文】
(台帳のカラム値 または OCR抽出テキスト)
...
```

**効果:**
1.  **文脈の明示:** 単なるテキストマッチではなく、「承認済みのレコードを探したい」といった意図に対して、メタデータ（ステータス等）がベクトル空間上で強く作用するようになる。
2.  **スキーマの簡素化:** `embedding_keyword` カラムの追加が不要となり、既存のMroongaベクトルインデックスをそのまま活用できる。

### 3.2. インデックス粒度の最適化 (Partial Re-indexing)

運用コストを最小化するため、台帳（Ledger）単位ではなく、**添付ファイル（AttachedFile）単位**でチャンクを管理・更新できる構造へ変更する。

#### データベーススキーマ変更 (`ledger_chunks`)

| カラム名 | 型 | 説明 | 変更内容 |
| :--- | :--- | :--- | :--- |
| `id` | bigInt | PK | - |
| `ledger_id` | bigInt | FK (ledgers) | - |
| **`attached_file_id`** | **bigInt** | **FK (attached_files)** | **新規追加 (Nullable, Index付)** |
| `chunk_text` | text | ベクトル化元のテキスト | メタデータ注入済みのテキストを格納 |
| `embedding` | vector | ベクトルデータ | 既存カラムを継続利用 |

#### 更新フロー
特定の添付ファイルのOCR処理が再実行された場合：
1.  `DELETE FROM ledger_chunks WHERE attached_file_id = ?`
2.  キーワード抽出・メタデータ生成・ベクトル化
3.  `INSERT INTO ledger_chunks ...`

---

## 4. 実装計画 (WBS)

### Phase 3.1: データ構造の最適化
**目的:** 添付ファイル単位のライフサイクル管理を実現する。

| ID | タスク名 | 詳細 | 担当 | 工数見積 |
| :--- | :--- | :--- | :--- | :--- |
| 3.1.1 | DBマイグレーション | `ledger_chunks` に `attached_file_id` を追加。 | Backend | 2h |
| 3.1.2 | モデルリレーション定義 | `LedgerChunk` と `AttachedFile` の関係を定義。 | Backend | 1h |

### Phase 3.2: メタデータ注入ロジックの実装
**目的:** Ruri v3の性能を最大化するチャンクを生成する。

| ID | タスク名 | 詳細 | 担当 | 工数見積 |
| :--- | :--- | :--- | :--- | :--- |
| 3.2.1 | キーワード抽出サービス | `logue/igo-php` を利用し、名詞・複合語・重要語句を抽出する `KeywordExtractionService` を実装。 | Backend | 4h |
| 3.2.2 | チャンクフォーマッタ | 抽出キーワードとファイルメタデータを、Ruriのプレフィックス仕様 (`config('rag.model.active.prefix.passage')`) に適合する形式で結合する処理を実装。<br>※台帳レコード用と添付ファイル用の両方に対応。 | Backend | 4h |
| 3.2.3 | ジョブ改修 (`ProcessLedgerForRagJob`) | 既存の「台帳単位全洗い替え」ロジックを廃止し、「添付ファイル単位の差分更新」および「台帳本体のみの更新」に対応するようリファクタリング。 | Backend | 8h |

### Phase 3.4: 既存データの移行 (Re-indexing)
**目的:** 新しいメタデータ注入型チャンクへの移行。
**※UI統合(旧Phase 3.3/3.4)は見送り**

| ID | タスク名 | 詳細 | 担当 | 工数見積 |
| :--- | :--- | :--- | :--- | :--- |
| 3.4.1 | バッチコマンド拡張 | 既存の `rag:chunk-existing-ledgers` コマンドを拡張。<br>1. 全データ再処理モード（メタデータ注入版）<br>2. 添付ファイルのみ再処理モード<br>これらを実装する。 | Backend | 4h |
| 3.4.2 | 移行実行と検証 | 本番データ量での所要時間計測と実行。 | Ops | - |

### Phase 3.5: テストの改修・強化
**目的:** 変更に伴うリグレッション防止と新機能の品質保証。

| ID | タスク名 | 詳細 | 担当 | 工数見積 |
| :--- | :--- | :--- | :--- | :--- |
| 3.5.1 | `ProcessLedgerForRagJobTest` 改修 | 1. チャンク生成時に `attached_file_id` が正しく保存されるかの検証を追加。<br>2. メタデータ注入後のテキストフォーマット（`【メタデータ】`等）の検証。<br>3. 添付ファイル単位の更新（Partial Update）時に、他のファイルのチャンクが消えていないかの検証。 | Backend | 4h |
| 3.5.2 | `RagSearchServiceTest` 確認 | メタデータを含むチャンクに対して、ステータス条件などの自然言語検索がヒットするかどうかのテストケースを追加（Ruriプレフィックス対応の確認）。 | Backend | 2h |
| 3.5.3 | `VlmRagIntegrationTest` 強化 | VLM処理結果（Markdown）が、最終的なチャンクのメタデータとして正しく反映されているか確認する結合テストを追加。 | Backend | 3h |
| 3.5.4 | 新規ユニットテスト作成 | 新設する `KeywordExtractionService` のユニットテストを作成し、`logue/igo-php` による抽出挙動を網羅的に検証。 | Backend | 2h |

---

## 5. 完了条件

1.  `ledger_chunks` テーブルに `attached_file_id` カラムが追加されている。
2.  生成されたチャンクテキストの冒頭に `検索文書: 【メタデータ】...` という形式でキーワード・ステータス・作成者などが含まれている。
3.  特定のファイルを更新した際、関連するチャンクのみがDELETE/INSERTされていることがログで確認できる。
4.  「承認済み」などのステータス条件を含む自然言語検索で、適切なレコードがヒットする（メタデータ埋め込みの効果確認）。
5.  関連するテストスイート（`tests/Feature/Rag/*`, `tests/Feature/Jobs/*`）が全てパスする。
