# VLM/RAG統合 - Phase2 VLM処理実装 WBS

**プロジェクト:** VLM/RAG統合 - Phase2: VLM処理実装

**目的:** VLMコンテナと連携し、添付ファイルの画像やPDFからMarkdown形式のテキストと構造化データを抽出し、データベースに保存する一連の処理を実装します。

**関連ドキュメント:**
- [VLM/RAG統合実装計画書（最終版）](../../architecture/vlm-rag-integration.md)
- [Phase1 基盤整備 WBS](./2025-11-03_phase1-wbs.md)

---

## Work Breakdown Structure

| ID | タスク | 担当者 | 見積工数 (人日) | 成果物 | 備考 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **1.0** | **VLMサービスクラス実装** | | **2.0** | | VLMコンテナとの通信を責務とするサービスクラスを実装します。 |
| 1.1 | `VlmClientService` の基本構造作成 | | 0.5 | `app/Services/VlmClientService.php` | VLMコンテナとの通信を行うサービスクラスの雛形を作成します。 |
| 1.2 | `extract` メソッド実装 | | 1.0 | `app/Services/VlmClientService.php` | ファイルパスを引数に取り、VLMコンテナのAPIを呼び出して解析結果を返します。HTTPクライアント、タイムアウト、リトライ設定を含みます。 |
| 1.3 | エラーハンドリング実装 | | 0.5 | `app/Services/VlmClientService.php` | API通信失敗、タイムアウト、VLMコンテナからのエラーレスポンスを適切にハンドリングします。 |
| **2.0** | **VLM処理ジョブ実装** | | **2.5** | | VLM処理を非同期で実行するためのキュージョブを実装します。 |
| 2.1 | `ProcessVlmExtraction` ジョブ作成 | | 0.5 | `app/Jobs/Ledger/ProcessVlmExtraction.php` | `ShouldQueue` を実装したジョブクラスを作成します。 |
| 2.2 | `handle` メソッド実装 | | 1.5 | `app/Jobs/Ledger/ProcessVlmExtraction.php` | `VlmClientService` を利用してVLM処理を実行し、成功時に `attached_files` テーブルの `vlm_markdown` 等を更新します。適切なログ出力も行います。 |
| 2.3 | エラーハンドリングとリトライ実装 | | 0.5 | `app/Jobs/Ledger/ProcessVlmExtraction.php` | ジョブ失敗時に `attached_files` のステータスを `VLM_FAILED` に更新し、フレームワークの機能でリトライ処理を設定します。 |
| **3.0** | **既存処理との統合** | | **1.0** | | 既存のファイル処理フローにVLM処理を組み込みます。 |
| 3.1 | `ProcessAttachedFile` ジョブ改修 | | 1.0 | `app/Jobs/Ledger/ProcessAttachedFile.php` | Tikaでのテキスト抽出完了後、`shouldProcessWithVlm` メソッドでVLM処理の要否を判定し、`ProcessVlmExtraction` ジョブをディスパッチするロジックを追加します。 |
| **4.0** | **テスト実装** | | **3.0** | | 実装した機能の品質を担保するためのテストを作成します。<br/>**v2.0更新:** 初期実装で発見された問題（Log::spy()のチャンネルモック失敗、waitUntilReady()のタイムアウト）に対処し、テスト戦略を見直し。 |
| 4.1 | `VlmClientService` ユニットテスト | | 1.0 | `tests/Unit/Services/VlmClientServiceTest.php` | `Http::fake()` を用いてVLM APIをモック化。`partialMock()`で`waitUntilReady()`をスタブ化し、タイムアウト待機を回避。ログ検証は省略し機能検証に集中。 |
| 4.2 | `ProcessVlmExtraction` ジョブテスト | | 1.0 | `tests/Feature/Jobs/ProcessVlmExtractionTest.php` | `Bus::fake()` と完全モックした`VlmClientService`を使用し、ジョブのDB更新、ステータス変更、後続ジョブディスパッチを検証。`RefreshDatabaseWithTenant`で自動ロールバック。 |
| 4.3 | VLM統合テスト | | 1.0 | `tests/Feature/Vlm/VlmIntegrationTest.php` | `ProcessAttachedFile`ジョブの条件分岐ロジック（VLM有効/無効、ファイルタイプ、Tika失敗時）を検証。`mock(TikaClient::class)`でTika挙動を制御。 |
| **5.0** | **ドキュメント更新** | | **0.5** | | |
| 5.1 | Phase2 WBSドキュメント作成 | | 0.5 | 本ドキュメント | 本検討結果をWBSドキュメントとして正式に保存します。 |
| | **合計** | | **9.0** | | |
